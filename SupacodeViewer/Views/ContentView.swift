import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("SupacodeViewer")
                .font(.largeTitle)
                .navigationTitle("Supacode")
        }
    }
}

#Preview {
    ContentView()
}
